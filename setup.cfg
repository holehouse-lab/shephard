# Note: coverage and pytest configuration live in pyproject.toml. flake8 has no
# pyproject support, so its config stays here alongside yapf's.
# define consistent style
[yapf]
COLUMN_LIMIT = 119
INDENT_WIDTH = 4
USE_TABS = False

# define consistent style
[flake8]
max-line-length = 119
